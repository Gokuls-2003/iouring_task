import 'package:flutter/material.dart';

class MoversPage extends StatelessWidget {
  const MoversPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Movers Page',
        style: TextStyle(fontSize: 24, color: Colors.white),
      ),
    );
  }
}
